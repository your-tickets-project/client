export interface EventType {
  title: string;
  sub_title?: string;
  sponsor?: string;
  followers?: number;
  fromDate: string;
  toDate: string;
  location: string;
  price: string;
  src: string;
  slug: string;
}
